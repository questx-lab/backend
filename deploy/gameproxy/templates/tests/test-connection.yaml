apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gameproxy.fullname" . }}-test-connection"
  labels:
    {{- include "gameproxy.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gameproxy.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
