apiVersion: v1
kind: Service
metadata:
  name: {{ include "notification_proxy.fullname" . }}
  labels:
    {{- include "notification_proxy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "notification_proxy.selectorLabels" . | nindent 4 }}
