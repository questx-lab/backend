apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "notification_proxy.fullname" . }}-test-connection"
  labels:
    {{- include "notification_proxy.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "notification_proxy.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
