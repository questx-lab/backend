apiVersion: v1
kind: Service
metadata:
  name: {{ include "gamecenter.fullname" . }}
  labels:
    {{- include "gamecenter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gamecenter.selectorLabels" . | nindent 4 }}
