apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gamecenter.fullname" . }}-test-connection"
  labels:
    {{- include "gamecenter.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gamecenter.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
