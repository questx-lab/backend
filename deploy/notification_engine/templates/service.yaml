apiVersion: v1
kind: Service
metadata:
  name: {{ include "notification_engine.fullname" . }}
  labels:
    {{- include "notification_engine.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "notification_engine.selectorLabels" . | nindent 4 }}
