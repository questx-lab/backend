apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "notification_engine.fullname" . }}-test-connection"
  labels:
    {{- include "notification_engine.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "notification_engine.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
